<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ajouter un Auteur</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            height: auto;
            margin: 0;
            padding: 20px;
        }

        h1 {
            font-size: 2.5rem;
            color: #007bff;
            margin-bottom: 20px;
        }

        .form-container {
            background-color: white;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            width: 100%;
            max-width: 600px;
            text-align: center;
        }

        .btn-custom-container {
            background-color: white; 
            color: #007bff; 
            padding: 15px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            text-align: center;
            cursor: pointer;
            transition: background-color 0.3s ease;
            margin-top: 20px;
            display: inline-block;
            width: 100%;
        }

        .btn-custom-container:hover {
            background-color: #C0E8D5;
            color: red;
        }

        .btn-text {
            font-size: 1rem;
            color: #007bff;
            text-decoration: none;
            border: none;
            background: none;
            cursor: pointer;
            width: 100%;
        }

        .form-group {
            margin-bottom: 15px;
            text-align: left;
        }

        .form-group label {
            display: block;
            margin-bottom: 5px;
            font-size: 1rem;
            color: #6c757d;
        }

        .form-group input,
        .form-group select {
            width: 100%;
            padding: 10px;
            border: 1px solid #ced4da;
            border-radius: 5px;
            font-size: 1rem;
        }

        .form-group input:focus,
        .form-group select:focus {
            border-color: #007bff;
            outline: none;
        }
    </style>
</head>

<body>
    <header>
        <h1>Ajouter un Auteur</h1>
    </header>
    <main>
        <div class="form-container">
            {{ form_start(form, {'attr': {'enctype': 'multipart/form-data'}}) }}
            
            <div class="form-group">
                {{ form_label(form.username) }}
                {{ form_widget(form.username) }}
            </div>

            <div class="form-group">
                {{ form_label(form.email) }}
                {{ form_widget(form.email) }}
            </div>

            <div class="form-group">
                {{ form_label(form.picture, 'Photo de l\'auteur') }}
                {{ form_widget(form.picture) }}
            </div>

            <div class="form-group">
                {{ form_label(form.nbBooks, 'Nombre de livres') }}
                {{ form_widget(form.nbBooks) }}
            </div>

            <div class="btn-custom-container">
                <button type="submit" class="btn-text">Ajouter</button>
            </div>
            
            {{ form_end(form) }}

            <div class="button-container">
            <a href="{{ path('list_authors') }}" class="btn-custom-container">
                <span class="btn-text">List  d'Auteur</span>
            </a>
            
        </div>
        </div>
    </main>
</body>

</html>
