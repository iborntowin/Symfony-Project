<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Welcome</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }
        .welcome-container {
            background-color: white;
            padding: 40px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            text-align: center;
            margin-bottom: 20px;
        }
        h1 {
            font-size: 2.5rem;
            color: #007bff;
        }
        p {
            font-size: 1.2rem;
            color: #6c757d;
        }
        .button-container {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin-top: 50px;
        }
        .btn-custom-container {
            background-color: white; 
            color: #007bff; 
            padding: 40px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            text-align: center;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }
        .btn-custom-container:hover {
            background-color: #C0E8D5;
            color: red;
        }
        .btn-custom-container form {
            margin: 0;
        }
        .btn-text {
            font-size: 1.5rem;
            color: #007bff; 
            text-decoration: none;
            border: none; 
            background: none; 
            cursor: pointer; 
        }
    </style>
</head>
<body>

    <div class="welcome-container">
        <h1>Welcome</h1>
        <p>We're glad to have you here! Enjoy your experience.</p>
    </div>

    <div class="button-container">
        <div class="btn-custom-container">
            <form action="{{ path('goToAuthors') }}" method="POST">
                <button type="submit" class="btn-text">List Authors</button>
            </form>
        </div>

        <div class="btn-custom-container">
            <form action="{{ path('app_livre') }}" method="GET">
                <button type="submit" class="btn-text">List Livres</button>
            </form>
        </div>
    </div>

</body>
</html>
